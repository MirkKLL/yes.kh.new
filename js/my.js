$(window).load(function() {
    //console.log( "ready!" );
	$('#blueimp-gallery').data('useBootstrapModal', false);
	$('#blueimp-gallery').data('fullScreen', true);
	$("#carousel-example-generic").css('width', $(window).width());
});